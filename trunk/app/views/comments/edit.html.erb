<h1>Editing comment</h1>

<% form_for([@entry,@comment]) do |f| %>
  <%= f.error_messages %>

  <p>
    <c1><%= f.label :comment %></c1><br />
    <%= f.text_area :comment %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Back', entry_path(@entry) %>
